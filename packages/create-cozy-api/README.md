# create-cozy-api

The official scaffolder for cozy-api projects.

## Usage

```
npx create-cozy-api my-app
```

Generates a new cozy-api project with sample workflow + node, installs
dependencies via the detected package manager, and prints next steps.
