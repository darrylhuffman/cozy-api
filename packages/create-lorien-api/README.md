# create-lorien-api

The official scaffolder for lorien-api projects.

## Usage

```
npx create-lorien-api my-app
```

Generates a new lorien-api project with sample workflow + node, installs
dependencies via the detected package manager, and prints next steps.
