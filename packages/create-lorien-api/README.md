# create-lorien

The official scaffolder for lorien-api projects.

## Usage

```
npx create-lorien my-app
```

Generates a new lorien-api project with the runtime, `lorien` CLI, IDE assets,
sample workflow + node, installs dependencies via the detected package manager,
and prints next steps.
